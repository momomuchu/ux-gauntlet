PASS: S1: The structural lane MUST wait for the page load event, document.fonts.ready to settle, then a 500ms DOM-mutation quiescence window observed via MutationObserver recording zero mutations, then the set of document.getAnimations({ subtree: true }) entries whose effect.getComputedTiming().iterations count is finite becoming empty so a finite CSS transition/animation cannot elapse mid-settle, every animation whose iteration count is Infinity excluded from this precondition, treated as settled, never blocking settle, then every img element rendered in the DOM plus every element resolving a CSS background-image reaching a decoded complete state via awaited img.decode() resolution together with a computed-style background-image load check so a paint-only image decode cannot straddle axe injection, the whole precondition capped at a fixed 10-second maximum wait (an unmet image-readiness gate falling into settle-timeout like any other unmet precondition), then inject the pinned axe-core script configured with rules { tabindex: { enabled: false } } into each operator-supplied route's rendered DOM via Playwright, then collect axe violations, incomplete results, passes as structured data. # CR1-M2 CR2-B4 CR2-B13 CR2-B18 CR3-B7 CR4-B3 CR4-B4 CR5-B2
PASS: S2: The axe-core dependency MUST be pinned to the exact version 4.12.1, recorded verbatim in every structural report's metadata block.
PASS: S3: The axe-core run MUST be configured with the ruleset tag set derived from config/wcag-target.default.json's target field, defaulting to the tags wcag2a, wcag2aa, wcag21aa, wcag22aa, best-practice, recorded in report metadata every run. # CR1-M8
PASS: S4: For every interactive component the lane MUST run the axe rules button-name, link-name, input-button-name, select-name, aria-required-attr, aria-valid-attr-value, aria-command-name to verify name plus role are exposed, with value exposed only where the component has user-settable state. # CR2-B9
PASS: S5: The lane MUST run axe color-contrast to flag any text below the 4.5:1 normal-text ratio, below the 3:1 large-text ratio, where large text is 18pt-plus (14pt-plus when bold), computed with no rounding so 4.499:1 fails.
PASS: S6: The lane MUST run the axe rules label, aria-input-field-name to flag any form input lacking a programmatic label.
PASS: S7: The lane MUST run axe landmark-one-main to flag a page not exposing exactly one main landmark, reported under a best-practice label at moderate impact, never as a WCAG success criterion.
PASS: S8: The lane MUST run axe region to flag any rendered content sitting outside every landmark region.
PASS: S9: The lane MUST scan every section element, every element carrying role region, for a missing accessible name sourced from aria-label, aria-labelledby, title, flagging each as the finding code unlabeled-landmark-section independent of whether the accessibility tree ever assigned that element a landmark role, the 8 recognized WAI-ARIA landmark types being banner, navigation, main, search, form, region, complementary, contentinfo. # CR5-M1
PASS: S10: The lane MUST report heading-order findings under a best-practice label visibly distinct from WCAG success-criterion violations, never as a WCAG 1.3.1 failure.
PASS: S10a: The lane MUST flag a heading-order finding whenever, walking headings in DOM order, a heading's level exceeds the immediately preceding heading's level by more than 1. # CR4-Mi2
PASS: S11: The lane MUST run a deterministic DOM check flagging any element carrying a positive tabindex value greater than 0 as an anti-pattern.
PASS: S12: The lane MUST verify the operator-declared expected-main-content anchor resolves to an element that is found on the page, sits inside the single main landmark, is the largest visible text block measured over the candidate set matching main, article, section, div, p, h1, excluding from the comparison pool every candidate that is an ancestor of the declared anchor element, excluding from the comparison pool every candidate that is a descendant of the declared anchor element so the anchor is never disqualified by a candidate it contains nor by a candidate that contains it, excluding any candidate whose computed style resolves display:none / visibility:hidden / zero bounding-box area, excluding any candidate whose own rendered text content (element.innerText, trimmed) has zero length, by rendered bounding-box area (getBoundingClientRect width times height, never character count), ties broken by DOM order. # CR1-M4 CR1-M7 CR2-M6 CR3-B3 CR4-B1 CR4-B2 CR4-M3
PASS: S13: The lane MUST refuse to produce a gated structural result when the operator supplies no expected-main-content selector, mirroring the persona lane's refusal to start without an operator-defined happy path.
PASS: S14: When the page exposes a count of main landmarks other than exactly 1, the expected-main-content containment check MUST fail closed with the report value cannot-evaluate-ambiguous-main, never a false pass, this failure suppressing the entire S12 found-inside-prominent evaluation for that page so no main-content-missing finding nor any other S12-derived finding is emitted. # CR2-Mn11
PASS: S15: For each audited route's single declared continue control (a flow step for the structural lane being synonymous with one route in the --path list per S39, one continue-control expectation per route), the lane MUST verify that control is a semantic control (a native button, link, input, an element exposing role button), is keyboard-focusable, carries an accessible name computed via axe-core's accname engine per S49, sourced from button-name/link-name/input-button-name/select-name for native controls, from aria-command-name for role-based controls, never an independent name computation nor the retired spike continue-no-name DOM check, the continue-control accessible-name failure mode covered exclusively by this S49-sourced axe accname check. # CR1-M5 CR2-B5 CR2-B9 CR5-M2 CR5-B4
PASS: S16: The lane MUST run the axe ARIA-state ruleset (aria-required-attr, aria-valid-attr-value, aria-required-children, aria-required-parent) to flag a custom widget whose aria-expanded, aria-selected, aria-checked state is invalid.
PASS: S17: Each axe-violation finding's severity MUST be a pure function of axe's own impact field, mapping critical to 4, serious to 3, moderate to 2, minor to 1. # CR1-B1 CR1-B3 CR1-M10
PASS: S18: An axe incomplete result MUST be mapped to severity 0, surfaced as a needs-manual-review entry, never dropped, never reported as a pass.
PASS: S19: The structural lane MUST assign severity with zero LLM judgment, so no persona-derived value, no model-derived value enters the lane.
PASS: S20: Two findings sharing the same axe rule ID plus the same target_element_identifier MUST be deduplicated into one finding entry. # CR4-M7
PASS: S21: Given an identical DOM snapshot, an identical pinned axe-core version 4.12.1, an identical ruleset configuration, an identical pinned browser version, a matching render_environment_id, once the S1 settle precondition has completed, the lane MUST emit a byte-identical finding record set (each record spanning finding_id, severity, result-type, impact) covering the axe violation results, the axe incomplete results, on every run, so an incomplete-to-violation severity flip on a stable finding_id is caught, so an impact drift on a stable finding_id that would flip the S53/D7 CI-block outcome is caught, this determinism guarantee scoped to apply only when render_environment_id matches across runs. # CR1-M2 CR1-M11 CR3-B7 CR4-M2 CR4-M4 CR5-B5
PASS: S22: The structural lane MUST emit a schema-validated JSON findings file in the same findings-schema family as the persona lane, carrying a mandatory top-level lane field, plus a mandatory per-finding lane field, both fixed to the string structural, so a genuinely clean zero-findings bundle still carries the structural discriminator. # CR3-B5
PASS: S23: The structural findings file MUST stay a separate artifact, never merged with the persona findings file into one blended score.
PASS: S24: A structural finding MUST cross-reference a persona finding only through the shared target_element_identifier field plus the shared run route field compared verbatim with no normalization on either side, the report disclosing no cross-lane match found for a route rather than silently emitting an empty join, never through a merged severity value. # CR1-B4 CR3-M7
PASS: S25: Every structural report MUST print a validity envelope stating that both the criteria-count figure of about 32 percent (16 of 50), the issue-volume figure of about 57 percent are vendor-reported by Deque, not independently audited, so a structural pass is not usable, is not good UI. # CR1-M6
PASS: S26: The validity envelope MUST name the WCAG criteria classes automated testing cannot evaluate, listing at minimum focus order, focus visible, keyboard operability.
PASS: S26b: The validity envelope MUST name contrast-exemption categories including logotype text, incidental text, text that is part of an inactive user interface component as a class automated contrast testing cannot reliably distinguish from a real violation. # CR4-M6, CR5-Mn2 sourced from DECISION-BRIEF-0002-STRUCTURAL.md source [2] (W3C Understanding SC 1.4.3) — brief body section 3 mirror still pending, not yet written
PASS: S26c: The validity envelope MUST disclose that the byte-identical determinism guarantee of S21, S42 holds only within a matching render_environment_id, that cross-render-environment reproduction is a disclosed residual the lane does not guarantee because host OS glyph shaping plus image-decode timing can flip a borderline bounding-box tie-break, a borderline color-contrast result, this residual being the stated reason S44 refuses to treat two reports with differing render_environment_id as comparable. # CR5-mandate
PASS: S27: The validity envelope MUST label heading-order findings as best-practice heuristics, never as WCAG success-criterion violations. # CR1-Mi2
PASS: S28: Every structural report's metadata MUST disclose the pinned axe-core version 4.12.1 plus the configured ruleset tags used for that run.
PASS: S29: Every structural report MUST state that a structural pass is not a WCAG conformance certification.
PASS: S30: The CI gate MUST block a merge whenever the structural lane emits any axe critical-impact violation, independent of the 0-to-4 severity mapping. # CR1-B3
PASS: S31: The WCAG target level MUST be a configurable value defaulting to AA, swappable through configuration data without editing lane logic.
PASS: S32: Any bump of the pinned axe-core version MUST require a recorded decision record before the new version is used.
PASS: S33: The structural lane MUST audit only the operator-supplied route list per run, performing no site-wide crawl, no autonomous route discovery.
PASS: S34: The structural lane MUST run independently of the persona lane with no cross-lane gating in the MVP, so neither lane's result gates whether the other lane runs.
PASS: S35: Each non-axe DOM-check finding's severity MUST be a fixed spec-pinned value keyed by finding code, mapping main-content-missing to 4, continue-control-missing to 4, main-content-not-in-main to 3, cannot-evaluate-ambiguous-main to 3, continue-not-semantic to 3, continue-not-focusable to 3, main-content-not-prominent to 2, unlabeled-landmark-section to 2, positive-tabindex to 1. # CR1-B1 CR1-B3
PASS: S36: An axe incomplete-derived finding MUST always take severity 0 per S18, that severity-0 assignment taking precedence over any impact value present on the incomplete entry. # CR1-M10
PASS: S37: The structural CI gate MUST block a merge whenever the findings file carries a run-level refused status per S13, independent of the axe critical-impact predicate. # CR1-B2
PASS: S38: The structural CI gate MUST block a merge whenever any non-axe DOM-check finding carries the code main-content-missing, the code continue-control-missing, evaluated on the persisted finding code directly rather than its derived severity integer so a corrupted S35 code-to-severity mapping cannot silently defeat the block (mirroring the raw-field reads of S52, S56), independent of the axe critical-impact predicate. # CR1-B3 CR5-B6
PASS: S39: The operator-declared expected-main-content configuration MUST be a route-to-selector map carrying one selector entry per audited route, keyed by exact case-sensitive match against the operator-supplied --path argument verbatim, applying no trailing-slash normalization, applying no query-string normalization. # CR1-M1 CR2-Mn8
PASS: S40: The structural lane MUST refuse a gated result for any audited route whose expected-main-content map carries no matching selector entry. # CR1-M1
PASS: S41: Two non-axe findings sharing the same finding code plus the same target_element_identifier MUST be deduplicated into one finding entry. # CR1-M3 CR4-M7
PASS: S42: The structural lane MUST emit a byte-identical non-axe custom-check finding record set (each record spanning finding_id, severity, code) across repeated runs on the same DOM snapshot under the same pinned axe-core version 4.12.1, the same pinned browser version, a matching render_environment_id across runs, this determinism guarantee scoped textually symmetric with sibling S21 to apply only when render_environment_id matches across runs. # CR1-M7 CR3-B7 CR5-B1 CR5-B3
PASS: S43: Any bump of the pinned Playwright browser binary version MUST require a recorded decision record before the new version is used. # CR1-M12
PASS: S44: A structural CI comparison MUST refuse to treat two reports as comparable whenever any of their axe_version, browser_version, ruleset_tags, render_environment_id metadata values differ between the two reports. # CR1-Mi5 CR2-M7 CR2-M19 CR3-M4
PASS: S45: The structural lane MUST record the run_status value axe-execution-failed for any route where the axe run fails to complete, still emitting the non-axe DOM-check findings (landmark, tabindex, main-content, continue-control) that completed before the failure, omitting only the axe-derived findings for that route. # CR1-B5 CR3-Mn4 CR4-Mi3
PASS: S46: The structural CI gate MUST block a merge whenever any route's run_status value is axe-execution-failed. # CR1-B5
PASS: S47: The structural lane MUST record the run_status value settle-timeout for any route where the S1 settle precondition does not complete within its fixed 10-second maximum wait. # CR2-B4
PASS: S48: The structural CI gate MUST block a merge whenever any route's run_status value is settle-timeout. # CR2-B4
PASS: S49: The accessible-name string that S15 requires MUST be computed via axe-core's exposed accname engine axe.commons.text.accessibleText, invoked inside an axe.setup(document) / axe.teardown() lifecycle bracket so accessibleText resolves against a live element, on the same pinned axe-core 4.12.1 build used for S4's rule checks, never a separate name computation. # CR2-B5 CR3-M2
PASS: S50: The finding_id MUST be computed as a pure deterministic function of the route, the axe rule ID / non-axe finding code, the target_element_identifier only, excluding any run-timestamp value, random value, DOM-node-reference component. # CR2-B16 CR4-M7
PASS: S51: The S21, S42, SN1 acceptance tests MUST invoke the actual finding_id generation function twice against one synthetic DOM fixture input, asserting the two returned finding-id sets are set-equal, beyond the gate-fixture comparator tests. # CR2-B16
PASS: S52: The structural-ci-diff.mjs CI gate MUST evaluate the S30 critical-impact block predicate against each axe-sourced finding's raw impact field, never against its derived severity integer. # CR2-B17
PASS: S53: The structural CI gate MUST block a merge whenever any axe incomplete finding's underlying impact field is critical, independent of the S18/S36 severity-0 display value. # CR2-M2
PASS: S54: The structural CI gate MUST block a merge whenever any audited route's findings carry the S40 route-level refused status. # CR2-M3
PASS: S55: For the one live defect-class pair in the cross-namespace equivalence table (the landmark-one-main axe rule ID paired with the cannot-evaluate-ambiguous-main finding code), when the axe finding of that rule ID appears together with its paired non-axe finding code for the same route, the non-axe finding MUST be emitted, the axe finding MUST be suppressed from the report; the tabindex/positive-tabindex pair is intentionally omitted because S1 disables the axe tabindex rule so no axe tabindex finding can ever reach this suppression. # CR2-M10 CR3-2 CR4-M1
PASS: S56: The structural CI gate MUST block a merge whenever any non-axe DOM-check finding carries the code cannot-evaluate-ambiguous-main, this fail-closed containment result treated as unmergeable like a refused run despite its pinned severity 3. # CR3-1
PASS: S57: The structural-ci-diff.mjs CI gate MUST refuse with a non-zero exit any input findings file whose metadata.axe_version is not the pinned 4.12.1 per S2, whose schema_version is not the supported value per SN2, whose top-level lane is not the string structural per S22, evaluated before any route-level CI-block predicate. # CR4-B5
PASS: S58: The structural lane MUST record the run_status value axe-execution-degraded for any route where axe.run() resolves yet the combined count of violation results, incomplete results, pass results is zero on a route whose pre-injection DOM snapshot contains at least one node matching the closed selector union a[href], button, input, select, textarea, [role=button], [role=link], [role=switch], [role=checkbox], [role=radio], [role=tab], [role=menuitem], [role=combobox], [role=slider], [role=textbox], S9's 8 landmark roles (banner, navigation, main, search, form, region, complementary, contentinfo), h1, h2, h3, h4, h5, h6, this selector list being the sole inline source of truth so no builder discretion remains. # CR4-M8 CR5-M4
PASS: S59: The structural CI gate MUST block a merge whenever any route's run_status value is axe-execution-degraded. # CR4-M8
PASS: S60: Every axe-sourced finding record, violation entries plus incomplete entries alike, MUST persist a raw impact string property in the findings schema's per-finding required array, distinct from the derived severity integer. # CR4-M9
PASS: S61: The target_element_identifier for non-axe DOM-check findings MUST be computed via the same priority-ordered stable-identifier algorithm SPEC 0001 pins in F103/F183/F193 (a test-id, aria-label, id tier first, falling back to a structural path anchored to the nearest ancestor carrying a stable id, terminating at documentElement when no ancestor carries one), applied identically across every non-axe finding code, never a DOM-index nth-child path nor any selector construction that varies with unrelated sibling presence, so the S42/S50/SN1 determinism guarantee cannot break via an unstable selector on byte-identical DOM. # CR5-M5
PASS: SN1: The structural lane MUST reproduce the same finding set across repeated runs on the same DOM snapshot under the same pinned version, with no run-to-run variance from DOM-node-reference instability. # CR1-Mi3
PASS: SN2: The structural findings JSON MUST conform to a versioned schema carrying a schema_version integer field the gate checks before accepting the file.
PASS: SN3: The structural lane MUST depend on no third-party network service beyond the local browser runtime when the target is a localhost route.
PASS: SN4: The structural lane MUST render each route at a single pinned desktop viewport of 1280x800 pixels. # CR1-Mi1
PASS: SN5: All shipped structural-lane artifacts MUST be MIT-licensed, authored in English.
PASS: SN6: The structural report metadata MUST record the run timestamp, the pinned axe-core version, the ruleset tags, the WCAG target level as machine-readable fields.
PASS: SN7: The structural report metadata MUST record the pinned Playwright browser binary version as a machine-readable field. # CR1-M12
PASS: SN8: The structural lane MUST run inside one documented container image pinning the OS version, the font-rendering-library version, recorded in report metadata as render_environment_id computed at lane-startup as a hash of the installed OS / font-rendering-library package versions rather than a hand-typed label, requiring a decision record to change it. # CR2-M20 CR3-M12
---
SUMMARY: 72 requirement(s) checked, 0 failed (mechanical check: testable/atomic/binding/unambiguous).
MANUAL CHECKLIST (not mechanically checkable — verify by hand before freeze, per CRITERION 1):
  - true stakeholder need: does each line trace to a real stated need, not an assumption?
  - stakeholder vocabulary: does each line use the domain's own words, not implementation jargon?
  - acceptable to all stakeholders: is there any unresolved objection to any line?
