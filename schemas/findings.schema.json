{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://ux-gauntlet/schemas/findings.schema.json",
  "title": "UX Gauntlet findings bundle",
  "type": "object",
  "required": ["run", "findings"],
  "properties": {
    "lane": {
      "description": "Lane discriminator (B9, R3 M15). EXPLICIT, not shape-sniffed: a bundle is persona-lane when lane is \"persona\" (the default assemble-run.mjs emits) or absent (frozen 0001-safe default). A structural-lane bundle (lane:\"structural\") must be routed to structural-report-gate.mjs; report-gate.mjs / ci-diff.mjs / render-report.mjs exact-match this discriminator and refuse a \"structural\" (or any other explicit) value. Kept OPTIONAL here (not in top-level required) so the structural schema is exactly this family's required set plus \"lane\" (0002 D6/M9).",
      "type": "string",
      "enum": ["persona"],
      "default": "persona"
    },
    "run": {
      "type": "object",
      "properties": {
        "route": { "type": "string" }
      }
    },
    "run_status": { "$ref": "#/$defs/run_status" },
    "personas": {
      "type": "array",
      "items": { "$ref": "#/$defs/persona_entry" }
    },
    "findings": {
      "type": "array",
      "items": { "$ref": "#/$defs/finding" }
    }
  },
  "$defs": {
    "run_status": {
      "type": "string",
      "enum": [
        "completed",
        "crashed",
        "timed-out",
        "patience-exhausted",
        "runner-capped",
        "BLOCKED"
      ]
    },
    "persona_entry": {
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "run_status": {
          "type": "string",
          "enum": [
            "completed",
            "crashed",
            "timed-out",
            "patience-exhausted",
            "runner-capped"
          ]
        },
        "action_count": { "type": "integer" },
        "tool_call_count": { "type": "integer" },
        "patience_threshold_steps": { "type": "integer" },
        "start_ts": { "type": "string" },
        "end_ts": { "type": "string" },
        "last_completed_step": { "type": "integer" }
      },
      "additionalProperties": true
    },
    "finding": {
      "type": "object",
      "required": [
        "friction_name",
        "friction_type",
        "step",
        "heuristic_tag",
        "personas_flagging",
        "convergence_tier",
        "severity",
        "severity_factors",
        "finding_id"
      ],
      "properties": {
        "id": { "type": "string" },
        "finding_id": { "type": "string" },
        "friction_name": { "type": "string", "minLength": 1 },
        "friction_type": {
          "type": "string",
          "enum": [
            "walkthrough_failure",
            "ambiguity_resolution",
            "extra_action",
            "terminal_friction"
          ]
        },
        "step": { "type": "integer" },
        "target_element_identifier": { "type": "string" },
        "target_selector": { "type": "string" },
        "concurrent_candidates": { "type": "integer" },
        "personas_flagging": {
          "type": "array",
          "items": { "type": "string" }
        },
        "convergence_tier": { "type": "integer", "minimum": 0 },
        "partial_tier": { "type": "integer", "minimum": 0 },
        "heuristic_tag": { "type": "string", "minLength": 1 },
        "severity": { "type": "integer", "minimum": 0, "maximum": 4 },
        "severity_factors": {
          "type": "object",
          "properties": {
            "frequency": {},
            "impact": {},
            "persistence": {}
          }
        },
        "component_severities": {
          "type": "array",
          "items": { "type": "integer", "minimum": 0, "maximum": 4 }
        },
        "confidence": { "type": "string" },
        "confidence_label": { "type": "string" },
        "standardized_flow": { "type": "string" },
        "market_impact": { "type": "boolean" },
        "evidence": { "type": "array" },
        "narrative": { "type": "string" }
      },
      "additionalProperties": true
    }
  }
}
